import { OptionsObject} from "notistack";

export const FAIL_TOP_MIDDLE_OPTION: OptionsObject = {
    variant: 'error',
    anchorOrigin: {
        horizontal: "center",
        vertical: "top"
    }
}

